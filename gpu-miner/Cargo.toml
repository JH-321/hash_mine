[package]
name = "gpu-miner"
version = "0.1.0"
edition = "2021"

[dependencies]
metal = "0.27"
tiny-keccak = { version = "2.0", features = ["keccak"] }

[profile.release]
opt-level = 3
lto = "fat"
codegen-units = 1
panic = "abort"
